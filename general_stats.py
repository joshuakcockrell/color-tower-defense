
import pygame
import player

'''Display settings'''
screen = pygame.display.set_mode((750, 600)) #Resolution of screen ((x, y))
pygame.display.set_caption('Color Tower Defense')
fps = 30
pygame.mouse.set_visible(True) #mouse visible
clock = pygame.time.Clock() #opens clock for fps
tile = 50 # size of every tile in pixels


        
